---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Tag
permissions: { contents: read }

on:
  schedule: [{ cron: '0 0 * * *' }]
  push: { branches: ["main"] }
  workflow_dispatch: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tag:
    if: github.repository_owner == 'mirceanton' # ? don't run on forks
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Generate Token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        id: app-token
        with:
          app-id: '${{ secrets.BOT_APP_ID }}'
          private-key: '${{ secrets.BOT_APP_PRIVATE_KEY }}'

      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          token: '${{ steps.app-token.outputs.token }}'

      - name: Determine next version
        id: tags
        uses: mirceanton/action-semver-release@fea37a9e8b01bdcba71c60327a8b02c6a0e844a5 # v3.0.2
        with:
          github-token: '${{ steps.app-token.outputs.token }}'

      - name: Create and push tag
        if: steps.tags.outputs.should-release == 'true' && github.event_name == 'schedule'
        run: |
          git config user.name "${{ steps.app-token.outputs.app-slug }}[bot]"
          git config user.email "${{ steps.app-token.outputs.installation-id }}+${{ steps.app-token.outputs.app-slug }}[bot]@users.noreply.github.com"
          git tag -a "${{ steps.tags.outputs.next-version }}" -m "Release ${{ steps.tags.outputs.next-version }}"
          git push origin "${{ steps.tags.outputs.next-version }}"
